{% extends "base.twig" %}
{% block title %}Modifier le profil - Kezan{% endblock %}
{% block page_title %}Modifier le profil{% endblock %}
{% block content %}

    <div class="add-content">
        {% if erreur %}
            <p style="color:red; margin-bottom: 1rem;">{{ erreur }}</p>
        {% endif %}

        <form class="add-form" method="post" action="/modifier_profil">

            <div class="form-group">
                <label>Prénom</label>
                <input type="text" name="prenom" value="{{ utilisateur.Prenom ?? '' }}" required>
            </div>

            <div class="form-group">
                <label>Nom</label>
                <input type="text" name="nom" value="{{ utilisateur.Nom_Utilisateur ?? '' }}" required>
            </div>

            <div class="form-group">
                <label>Email</label>
                <input type="email" name="email" value="{{ utilisateur.Email ?? '' }}" required>
            </div>

            <div class="form-group">
                <label>Téléphone</label>
                <input type="tel" name="telephone" value="{{ utilisateur.Telephone ?? '' }}">
            </div>

            <div class="form-group">
                <label>Nouveau mot de passe <span style="color:#999; font-size:0.8rem;">(laisser vide pour ne pas changer)</span></label>
                <input type="password" name="mot_de_passe">
            </div>

            <div class="form-group">
                <label>Confirmer le mot de passe</label>
                <input type="password" name="mot_de_passe_confirmation">
            </div>

            <div style="display: flex; gap: 1rem;">
                <a href="/profil" class="btn-secondary add-submit" style="text-align: center; text-decoration: none;">
                    Annuler
                </a>
                <button type="submit" class="btn-primary add-submit">Enregistrer</button>
            </div>

        </form>
    </div>

{% endblock %}