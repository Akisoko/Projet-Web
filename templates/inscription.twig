{% extends "base_connexion.twig" %}
{% block title %}Inscription - Kezan{% endblock %}
{% block content %}

    <div class="auth-container signup-page">
        <div class="auth-form-card signup-card">
            <div class="signup-header">
                <div class="logo-container small">
                    <img src="https://img.icons8.com/color/512/world-of-warcraft-horde.png" alt="Kezan Logo" class="landing-logo">
                </div>
                <div>
                    <h2>Créer un compte</h2>
                    <p class="auth-subtitle">Inscrivez vous pour commencer</p>
                </div>
            </div>

            {% if erreur %}
                <p style="color: red; margin-bottom: 1rem;">{{ erreur }}</p>
            {% endif %}

            <form class="auth-form signup-form" method="POST" action="/inscription">

                {% if auth_role == 1 or auth_role == 3 %}
                    <div class="form-row">
                        <div class="form-group" style="width: 100%;">
                            <label>Rôle pour ce nouveau compte</label>
                            <select name="role_cree" class="select-field" style="width: 100%; padding: 0.75rem; border: 1px solid #ddd; border-radius: 8px; font-size: 0.95rem; margin-bottom: 1rem;">
                                <option value="2">Étudiant</option>
                                {% if auth_role == 1 %}
                                    <option value="3">Pilote</option>
                                {% endif %}
                            </select>
                        </div>
                    </div>
                {% endif %}

                <div class="form-row">
                    <div class="form-group">
                        <label>Civilité</label>
                        <div class="radio-group">
                            <label class="radio-label">
                                <input type="radio" name="civilite" value="mr" checked>
                                <span>Mr</span>
                            </label>
                            <label class="radio-label">
                                <input type="radio" name="civilite" value="mme">
                                <span>Mme</span>
                            </label>
                        </div>
                    </div>
                    <div class="form-group">
                        <label>Email</label>
                        <input type="email" name="email" required>
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label>Nom</label>
                        <input type="text" name="nom" required>
                    </div>
                    <div class="form-group">
                        <label>Téléphone</label>
                        <input type="tel" name="telephone" required>
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label>Prénom</label>
                        <input type="text" name="prenom" required>
                    </div>
                    <div class="form-group">
                        <label>Mot de passe</label>
                        <input type="password" name="mot_de_passe" required>
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label>Date de naissance</label>
                        <div class="date-group">
                            <select name="jour" required>
                                <option value="">JJ</option>
                                {% for j in 1..31 %}
                                    <option value="{{ j < 10 ? '0' ~ j : j }}">{{ j < 10 ? '0' ~ j : j }}</option>
                                {% endfor %}
                            </select>
                            <select name="mois" required>
                                <option value="">MM</option>
                                {% for m in 1..12 %}
                                    <option value="{{ m < 10 ? '0' ~ m : m }}">{{ m < 10 ? '0' ~ m : m }}</option>
                                {% endfor %}
                            </select>
                            <select name="annee" required>
                                <option value="">AAAA</option>
                                {% for a in 1990..2010 %}
                                    <option value="{{ a }}">{{ a }}</option>
                                {% endfor %}
                            </select>
                        </div>
                    </div>
                    <div class="form-group">
                        <label>Confirmation mot de passe</label>
                        <input type="password" name="mot_de_passe_confirmation" required>
                    </div>
                </div>

                <button type="submit" class="btn-primary auth-submit">S'inscrire</button>
            </form>

            <p class="auth-footer">
                {% if not auth_role %}
                    Déjà un compte ? <a href="/connexion">Se connecter</a>
                {% else %}
                    <a href="/accueil">Retour à l'accueil</a>
                {% endif %}
            </p>
        </div>
    </div>

{% endblock %}